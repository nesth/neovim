{
  plugins.avante = {
    enable = true;
    settings = {
      provider = "gemini";
      auto_suggestions_provider = "gemini";
      behaviour = {
        auto_suggestions = true;

      };
    };
  };
}
