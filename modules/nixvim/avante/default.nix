{
  plugins = {
    avante-nvim = {
      enable = true;
      provider = "gemini";
      auto_suggestion_provider = "gemini";
      settings = {
        gemini = {
          enable = true;
        };
      };
    };
  };
}
